# relu: the reasoning here is that the relu is subjectively the most
# common/default activation function in DNNs, but I don't LOVE this
# DEFAULT_ACT = {"type": "relu"}
IGNORE_HASH_KEYS = ["model_hash"]
