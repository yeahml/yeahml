# all paths are relative to yaml/execution?
# TODO: naming/structure will need to be improved
overall:
  verbosity: 0
  tf_log_level: 'warn'
  # will need to investigate disabling cudnn > nondeterministic backprop
  deterministic: null
  rand_seed: null # TODO: use
  # regression, segmentation, GAN, autoencode
  type: 'classification' # TODO: use
  options: 'binary'
  seed: 42
  # performance:
  #   # default (auc, acc,loss)
  #   eval:
  #     - 'sensitivity'
  #     - 'precision'
  #     - 'F1'
data:
  in_dim: [150, 150, 3]
  reshape_in_to: null
  directory: null
  output_dim: [1]
  TFR_dir: './experiment/cats_v_dogs_01/data/record_holder/150'
  # TODO: train/val/test split, kfold?
  # TODO: augmentation?
  # TODO: preprocessing
saver:
  # pickel-ed-params
  save_pparams: 'best_params'
  load_pparams: null
  save_graph: null
  # if load model, then the architecture+implementation is void
  load_graph: null
tensorboard:
  # if null, a default location will be created/selected
  log_dir: null
hyper_parameters:
  lr: 0.0001
  batch_size: 16
  epochs: 3
architecture:
  # if yaml, then ignore the rest
  # type, size, details(filter), frozen, dropout, regularization, etc..
  # TODO: maybe this could also be a .py file containing a graph
  # > (clear input/output would need to be defined)
  yaml: './experiment/cats_v_dogs_01/architecture.yaml'
  #yaml: 'architecture.yaml'
implementation:
  # optimizer will be a string
  # TODO: will have 'sane' defaults
  optimizer: 'adam'
  initialization: null
  default_activation: 'elu'
  shuffle_buffer: 128
train:
  # the values aren't being used yet...
  image: 'standardize'
  augmentation:
    v_flip: 50
    h_flip: 50
    rotate: 50
    brightness: 0.2
