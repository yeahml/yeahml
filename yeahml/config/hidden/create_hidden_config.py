from yeahml.config.helper import parse_yaml_from_path


def extract_hidden_dict_and_set_defaults(h_raw_config: dict) -> dict:
    # TODO: check that the config has only approved values
    # create architecture config

    # TODO: this is only a place holder...
    h_config = h_raw_config

    return h_config
